# Intermediate Training Projects

This directory contains intermediate CMake and Make training projects that build on basic concepts.

## Project Structure
- `calculator/` - Multi-file project with tests
- `logger/` - Custom logging library with configuration
- `data_processor/` - Data processing library with plugins
- `network_client/` - Network client with external dependencies