# CMake Modules

This directory contains reusable CMake modules that can be used across different projects.

## Available Modules
- `FindCustomLib.cmake` - Example of a custom find module
- `Utils.cmake` - Utility functions for common tasks
- `PackageBuilder.cmake` - Helper for creating distribution packages
- `CodeCoverage.cmake` - Tools for measuring code coverage